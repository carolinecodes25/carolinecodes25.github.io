// This file contains the JavaScript code for the portfolio webpage.
// You can add interactivity, animations, or dynamic content loading here.

document.addEventListener('DOMContentLoaded', () => {
    console.log('Portfolio webpage is ready!');
    
    // Example: Add a click event to a button
    const button = document.getElementById('myButton');
    if (button) {
        button.addEventListener('click', () => {
            alert('Button clicked!');
        });
    }
});